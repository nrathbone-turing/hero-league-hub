// File: frontend/src/components/Heroes.jsx
// Purpose: Dynamic hero pool browser with search, pagination, and filters.
// Notes:
// - Replaces static Characters from P1.
// - Fetches from /api/heroes (paginated).
// - Reuses P1 stat card UI for hero display.

function Heroes() {
  // state: heroes, loading, error, search, page, totalPages, filters
  // effect: fetch heroes on page/search/filter change
  // render: 
  //   - search bar
  //   - filter controls (dropdowns, checkboxes)
  //   - hero grid (cards)
  //   - pagination controls
}
